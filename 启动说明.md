# 🚀 启动说明 - 详细步骤

本文档详细说明如何启动前端和后端服务。

---

## 📋 前置准备

### 1. 安装 Node.js

- 下载地址：https://nodejs.org/
- 选择 LTS 版本（推荐 20.x）
- 安装后验证：
  ```bash
  node --version
  npm --version
  ```

### 2. 安装 MySQL

- 下载地址：https://dev.mysql.com/downloads/mysql/
- 安装时记住 root 密码
- 确保 MySQL 服务正在运行

### 3. 配置 .env 文件

在项目根目录创建 `.env` 文件（如果不存在）：

```env
PORT=3001
API_TOKEN=123456789

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=你的MySQL密码
DB_NAME=smarthive
DB_PORT=3306

QWEN_API_KEY=你的通义千问API密钥（可选）
```

---

## 🎯 启动方式

### 方式一：一键启动（最简单）

**Windows 用户：**

1. 双击 `启动项目.bat`
2. 等待依赖安装完成（首次运行）
3. 配置数据库密码（如果提示）
4. 浏览器访问：http://localhost:5173

**优点：**
- 自动检查环境
- 自动安装依赖
- 自动创建配置文件
- 同时启动前后端

---

### 方式二：使用 npm start

**步骤：**

1. 打开命令行（CMD 或 PowerShell）
2. 进入项目目录：
   ```bash
   cd A:\agent\-
   ```
3. 安装依赖（首次运行）：
   ```bash
   npm install
   ```
4. 启动项目：
   ```bash
   npm start
   ```
5. 访问：http://localhost:5173

**说明：**
- 前端和后端会在同一个窗口显示
- 前端日志显示为绿色
- 后端日志显示为蓝色
- 按 `Ctrl+C` 停止服务

---

### 方式三：分别启动（适合调试）

#### 启动后端服务器

1. **打开第一个命令行窗口**
2. **进入项目目录：**
   ```bash
   cd A:\agent\-
   ```
3. **启动后端：**
   ```bash
   npm run dev:server
   ```
4. **看到以下信息表示成功：**
   ```
   ========================================
     智慧蜂场管理系统 - 后端服务器
   ========================================
   服务器地址: http://0.0.0.0:3001
   本地访问: http://localhost:3001
   ```

#### 启动前端服务器

1. **打开第二个命令行窗口**（新窗口）
2. **进入项目目录：**
   ```bash
   cd A:\agent\-
   ```
3. **启动前端：**
   ```bash
   npm run dev
   ```
4. **看到以下信息表示成功：**
   ```
   VITE v5.x.x  ready in xxx ms
   ➜  Local:   http://localhost:5173/
   ➜  Network: use --host to expose
   ```

#### 访问系统

- 前端地址：http://localhost:5173
- 后端API：http://localhost:3001/api/health

---

## 🔍 验证服务是否正常

### 检查后端

在浏览器访问：http://localhost:3001/api/health

**正常响应：**
```json
{
  "status": "ok",
  "databaseConnected": true,
  "timestamp": "2024-01-01T12:00:00.000Z"
}
```

### 检查前端

在浏览器访问：http://localhost:5173

**正常情况：**
- 页面正常加载
- 显示连接界面或数据界面
- 浏览器控制台（F12）无错误

---

## ⚠️ 常见启动问题

### 问题1：端口被占用

**错误信息：**
```
Error: listen EADDRINUSE: address already in use :::3001
```

**解决方法：**

1. **修改后端端口：**
   - 编辑 `.env` 文件
   - 修改 `PORT=3001` 为其他端口（如 `PORT=3002`）

2. **修改前端端口：**
   - 编辑 `vite.config.ts`
   - 修改 `port: 5173` 为其他端口（如 `port: 5174`）

3. **或者关闭占用端口的程序：**
   ```bash
   # Windows 查看端口占用
   netstat -ano | findstr :3001
   
   # 结束进程（替换PID为实际进程ID）
   taskkill /PID 进程ID /F
   ```

### 问题2：数据库连接失败

**错误信息：**
```
Database connection error
ER_ACCESS_DENIED_ERROR
```

**解决方法：**

1. **检查 MySQL 服务是否运行：**
   - Windows：打开"服务"管理器，查找 MySQL
   - 或命令行：`net start MySQL`

2. **检查 .env 配置：**
   - 确认 `DB_PASSWORD` 正确
   - 确认 `DB_HOST`、`DB_USER`、`DB_PORT` 正确

3. **测试数据库连接：**
   ```bash
   mysql -u root -p
   # 输入密码，如果成功进入说明数据库正常
   ```

### 问题3：依赖安装失败

**解决方法：**

1. **清除缓存：**
   ```bash
   npm cache clean --force
   ```

2. **删除 node_modules 和 package-lock.json：**
   ```bash
   rmdir /s node_modules
   del package-lock.json
   ```

3. **重新安装：**
   ```bash
   npm install
   ```

### 问题4：找不到模块

**错误信息：**
```
Cannot find module 'xxx'
```

**解决方法：**

1. **确保已安装依赖：**
   ```bash
   npm install
   ```

2. **检查 node_modules 是否存在：**
   ```bash
   dir node_modules
   ```

3. **如果不存在，重新安装：**
   ```bash
   npm install
   ```

---

## 📝 启动后的操作

### 插入测试数据

如果想快速看到数据效果：

```bash
# 方式一：使用批处理文件
插入测试数据.bat

# 方式二：使用命令行
node insertHiveTestData.js
```

### 查看日志

**后端日志：**
- 在启动后端的命令行窗口查看

**前端日志：**
- 在启动前端的命令行窗口查看
- 或浏览器控制台（F12）

---

## 🛑 停止服务

### 方式一：在命令行窗口

按 `Ctrl+C`，然后输入 `Y` 确认

### 方式二：关闭命令行窗口

直接关闭启动服务的命令行窗口（不推荐，可能导致资源未释放）

---

## 💡 提示

1. **首次启动较慢**：需要安装依赖，请耐心等待
2. **保持窗口打开**：关闭命令行窗口会停止服务
3. **修改代码后**：前端会自动刷新，后端需要重启
4. **生产环境**：不要使用 `npm start`，应该构建后部署

---

## 📞 需要帮助？

如果遇到问题：

1. 查看本文档的"常见启动问题"部分
2. 检查命令行窗口的错误信息
3. 查看 README.md 的常见问题部分
4. 确认所有前置条件都已满足

祝您使用顺利！🐝

