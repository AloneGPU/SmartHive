# 🚀 智慧蜂场管理系统 - 完整部署指南

本指南将详细说明如何在本地运行项目，以及如何部署到云服务器上。

---

## 📋 目录

1. [本地运行（开发环境）](#本地运行开发环境)
2. [云服务器部署](#云服务器部署)
3. [常见问题排查](#常见问题排查)
4. [安全配置建议](#安全配置建议)

---

## 🖥️ 本地运行（开发环境）

### 方式一：一键启动（推荐，最简单）

1. **双击运行 `启动项目.bat`**
   - 脚本会自动检查 Node.js
   - 自动安装依赖（首次运行）
   - 自动创建 `.env` 配置文件（如果不存在）
   - 同时启动前端和后端服务器

2. **配置数据库密码**
   - 如果首次运行，会创建 `.env` 文件
   - 用记事本打开 `.env` 文件
   - 修改 `DB_PASSWORD=你的MySQL密码`

3. **访问系统**
   - 浏览器会自动打开，或手动访问：http://localhost:5173
   - 后端API地址：http://localhost:3001

### 方式二：手动启动（适合调试）

#### 启动后端服务器

1. 打开第一个命令行窗口（CMD 或 PowerShell）
2. 进入项目目录：
   ```bash
   cd A:\agent\-
   ```
3. 启动后端：
   ```bash
   npm run dev:server
   ```
4. 看到以下信息表示启动成功：
   ```
   Backend server running on http://localhost:3001
   ```

#### 启动前端服务器

1. 打开第二个命令行窗口
2. 进入项目目录：
   ```bash
   cd A:\agent\-
   ```
3. 启动前端：
   ```bash
   npm run dev
   ```
4. 看到以下信息表示启动成功：
   ```
   VITE v5.x.x  ready in xxx ms
   ➜  Local:   http://localhost:5173/
   ```

#### 访问系统

- 前端地址：http://localhost:5173
- 后端地址：http://localhost:3001

### 方式三：使用 npm start（同时启动前后端）

```bash
npm start
```

这会同时启动前端和后端，但两个服务会在同一个窗口显示日志。

---

## ☁️ 云服务器部署

### 第一步：准备云服务器

#### 1.1 购买云服务器

推荐云服务商：
- **阿里云**：https://www.aliyun.com/
- **腾讯云**：https://cloud.tencent.com/
- **华为云**：https://www.huaweicloud.com/

**配置建议**（适合小型项目）：
- CPU：2核
- 内存：4GB
- 系统盘：40GB
- 操作系统：**Ubuntu 22.04 LTS** 或 **CentOS 7/8**（推荐 Ubuntu）

#### 1.2 连接服务器

**Windows 用户使用 PuTTY 或 PowerShell：**

```bash
# 使用 PowerShell SSH 连接
ssh root@你的服务器IP地址
```

**首次连接会提示确认，输入 `yes`**

#### 1.3 更新系统（Ubuntu）

```bash
# 更新软件包列表
apt update

# 升级系统
apt upgrade -y
```

---

### 第二步：安装必要软件

#### 2.1 安装 Node.js

```bash
# 方法一：使用 NodeSource 安装（推荐）
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# 验证安装
node --version  # 应该显示 v20.x.x
npm --version   # 应该显示 10.x.x
```

#### 2.2 安装 MySQL

```bash
# 安装 MySQL
apt install -y mysql-server

# 启动 MySQL 服务
systemctl start mysql
systemctl enable mysql  # 设置开机自启

# 安全配置（设置 root 密码）
mysql_secure_installation
```

**重要提示**：
- 记住您设置的 MySQL root 密码
- 选择 "Y" 移除匿名用户
- 选择 "Y" 禁止 root 远程登录（安全考虑）
- 选择 "Y" 移除测试数据库

#### 2.3 安装 Nginx（用于部署前端）

```bash
# 安装 Nginx
apt install -y nginx

# 启动 Nginx
systemctl start nginx
systemctl enable nginx

# 检查状态
systemctl status nginx
```

#### 2.4 安装 PM2（用于管理后端进程）

```bash
# 全局安装 PM2
npm install -g pm2

# 验证安装
pm2 --version
```

---

### 第三步：上传项目文件

#### 3.1 在本地构建项目

在您的本地电脑上（Windows）：

```bash
# 进入项目目录
cd A:\agent\-

# 构建前端
npm run build

# 构建后端
npm run build:server
```

#### 3.2 上传文件到服务器

**方法一：使用 WinSCP（图形界面，推荐小白）**

1. 下载 WinSCP：https://winscp.net/
2. 安装并打开 WinSCP
3. 新建连接：
   - 主机名：您的服务器IP
   - 用户名：root
   - 密码：您的服务器密码
   - 协议：SFTP
4. 连接后，将以下文件/文件夹上传到服务器的 `/root/smarthive` 目录：
   - `dist/` 文件夹（前端构建产物）
   - `dist-server/server.js`（后端构建产物）
   - `.env` 文件（需要修改配置）
   - `package.json`（用于安装依赖）

**方法二：使用 SCP 命令（命令行）**

在本地 PowerShell 中：

```bash
# 创建服务器目录
ssh root@你的服务器IP "mkdir -p /root/smarthive"

# 上传前端文件
scp -r dist root@你的服务器IP:/root/smarthive/

# 上传后端文件
scp dist-server/server.js root@你的服务器IP:/root/smarthive/
scp package.json root@你的服务器IP:/root/smarthive/
```

---

### 第四步：配置服务器

#### 4.1 修改 .env 文件

在服务器上编辑 `.env` 文件：

```bash
# 使用 nano 编辑器（简单易用）
nano /root/smarthive/.env
```

修改以下配置：

```env
# 后端服务器配置
PORT=3001
API_TOKEN=你的复杂密码（不要用默认的123456789）

# MySQL数据库配置
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=你的MySQL密码
DB_NAME=smarthive
DB_PORT=3306

# 通义千问 API（可选）
QWEN_API_KEY=你的API密钥
```

**保存文件**：按 `Ctrl+O`，然后按 `Enter`，最后按 `Ctrl+X` 退出

#### 4.2 安装后端依赖

```bash
cd /root/smarthive
npm install --production
```

---

### 第五步：配置 Nginx（前端）

#### 5.1 创建 Nginx 配置

```bash
# 编辑 Nginx 配置
nano /etc/nginx/sites-available/smarthive
```

添加以下内容：

```nginx
server {
    listen 80;
    server_name 你的服务器IP或域名;

    # 前端静态文件
    root /root/smarthive/dist;
    index index.html;

    # 前端路由
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 代理
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

**保存文件**：`Ctrl+O` → `Enter` → `Ctrl+X`

#### 5.2 启用配置

```bash
# 创建符号链接
ln -s /etc/nginx/sites-available/smarthive /etc/nginx/sites-enabled/

# 删除默认配置（可选）
rm /etc/nginx/sites-enabled/default

# 测试配置
nginx -t

# 重新加载 Nginx
systemctl reload nginx
```

---

### 第六步：启动后端服务（使用 PM2）

#### 6.1 创建 PM2 启动脚本

```bash
cd /root/smarthive
nano ecosystem.config.js
```

添加以下内容：

```javascript
module.exports = {
  apps: [{
    name: 'smarthive-backend',
    script: './server.js',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '500M',
    env: {
      NODE_ENV: 'production'
    }
  }]
};
```

#### 6.2 启动服务

```bash
# 启动服务
pm2 start ecosystem.config.js

# 设置开机自启
pm2 startup
pm2 save

# 查看服务状态
pm2 status

# 查看日志
pm2 logs smarthive-backend
```

---

### 第七步：配置防火墙

```bash
# Ubuntu 使用 ufw
ufw allow 22/tcp    # SSH
ufw allow 80/tcp   # HTTP
ufw allow 443/tcp  # HTTPS（如果使用SSL）
ufw enable

# 查看防火墙状态
ufw status
```

---

### 第八步：访问系统

1. **在浏览器访问**：`http://你的服务器IP`
2. **检查后端API**：`http://你的服务器IP/api/health`

如果看到健康检查返回正常，说明部署成功！

---

## 🔧 常见问题排查

### 问题1：无法访问网站

**检查步骤：**

1. **检查 Nginx 是否运行**
   ```bash
   systemctl status nginx
   ```

2. **检查后端是否运行**
   ```bash
   pm2 status
   ```

3. **检查防火墙**
   ```bash
   ufw status
   ```

4. **查看 Nginx 错误日志**
   ```bash
   tail -f /var/log/nginx/error.log
   ```

### 问题2：数据库连接失败

**检查步骤：**

1. **检查 MySQL 是否运行**
   ```bash
   systemctl status mysql
   ```

2. **测试数据库连接**
   ```bash
   mysql -u root -p
   # 输入密码后，如果成功进入 MySQL 命令行，说明数据库正常
   ```

3. **检查 .env 文件配置**
   ```bash
   cat /root/smarthive/.env
   ```

4. **查看后端日志**
   ```bash
   pm2 logs smarthive-backend
   ```

### 问题3：前端显示空白或404

**解决方法：**

1. **检查前端文件是否存在**
   ```bash
   ls -la /root/smarthive/dist
   ```

2. **检查 Nginx 配置**
   ```bash
   nginx -t
   ```

3. **清除浏览器缓存**，重新访问

### 问题4：PM2 服务无法启动

**解决方法：**

1. **查看详细错误**
   ```bash
   pm2 logs smarthive-backend --err
   ```

2. **检查文件权限**
   ```bash
   chmod +x /root/smarthive/server.js
   ```

3. **手动测试运行**
   ```bash
   cd /root/smarthive
   node server.js
   ```

---

## 🔐 安全配置建议

### 1. 修改默认 Token

**必须修改** `.env` 文件中的 `API_TOKEN`，使用复杂密码：

```env
API_TOKEN=你的复杂密码（至少16位，包含字母、数字、符号）
```

### 2. 配置 SSL 证书（HTTPS）

**使用 Let's Encrypt 免费证书：**

```bash
# 安装 Certbot
apt install -y certbot python3-certbot-nginx

# 申请证书（需要域名）
certbot --nginx -d 你的域名.com

# 自动续期
certbot renew --dry-run
```

### 3. 数据库安全

- 不要使用弱密码
- 定期备份数据库
- 限制数据库远程访问（只允许 localhost）

### 4. 服务器安全

- 定期更新系统：`apt update && apt upgrade`
- 使用 SSH 密钥登录（禁用密码登录）
- 配置 fail2ban 防止暴力破解

---

## 📝 日常维护

### 查看服务状态

```bash
# 查看后端状态
pm2 status

# 查看 Nginx 状态
systemctl status nginx

# 查看 MySQL 状态
systemctl status mysql
```

### 重启服务

```bash
# 重启后端
pm2 restart smarthive-backend

# 重启 Nginx
systemctl restart nginx

# 重启 MySQL
systemctl restart mysql
```

### 查看日志

```bash
# 后端日志
pm2 logs smarthive-backend

# Nginx 访问日志
tail -f /var/log/nginx/access.log

# Nginx 错误日志
tail -f /var/log/nginx/error.log
```

### 备份数据库

```bash
# 备份数据库
mysqldump -u root -p smarthive > backup_$(date +%Y%m%d).sql

# 恢复数据库
mysql -u root -p smarthive < backup_20240101.sql
```

---

## 💡 给小白的重要提示

1. **先本地测试**：在部署到云服务器之前，确保本地能正常运行
2. **保存配置**：记录好所有密码和配置信息
3. **定期备份**：定期备份数据库，防止数据丢失
4. **遇到问题**：先查看日志，大多数问题都能从日志中找到原因
5. **不要慌张**：服务器出问题很正常，重启服务通常能解决大部分问题

---

## 📞 需要帮助？

如果遇到问题：
1. 先查看本文档的"常见问题排查"部分
2. 查看服务器日志（PM2、Nginx、MySQL）
3. 检查配置文件是否正确
4. 确认所有服务都在运行

祝您部署顺利！🐝

