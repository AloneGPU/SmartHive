# 数据映射与完整性保障方案

更新时间：2026-04-02  
适用版本：当前仓库 `services/iotBridge.ts`、`services/dataMappingValidator.ts`、`services/databaseService.ts`

## 一、数据流程概览

```
树莓派采集 → MQTT上报 → 后端接收 → 数据库存储 → SSE推送 → 前端展示
    ↓           ↓           ↓           ↓           ↓           ↓
 传感器      JSON格式    字段映射     双表存储    实时推送    可视化
```

## 二、树莓派数据字段

### 2.1 传感器数据字段
| 字段名 | 类型 | 单位 | 描述 | 示例值 |
|--------|------|------|------|--------|
| `temp` | number | °C | 蜂箱内部温度（兼容字段） | 35.2 |
| `humi` | number | % | 蜂箱内部湿度（兼容字段） | 65.5 |
| `in_temp` | number | °C | 蜂箱内部温度 | 35.2 |
| `in_humi` | number | % | 蜂箱内部湿度 | 65.5 |
| `out_temp` | number | °C | 蜂箱外部温度 | 28.5 |
| `out_humi` | number | % | 蜂箱外部湿度 | 75.0 |
| `weight` | number | kg | 蜂箱重量 | 45.8 |
| `in_count` | number | - | 蜜蜂进入计数 | 120 |
| `out_count` | number | - | 蜜蜂出去计数 | 115 |
| `hornet_count` | number | - | 胡蜂检测数量 | 2 |
| `lat` | number | ° | 纬度 | 25.234489 |
| `lon` | number | ° | 经度 | 103.008597 |
| `fps` | number | fps | 视觉识别帧率 | 15.5 |
| `latency_ms` | number | ms | 推理延迟 | 45.2 |

**注意**：
- `temp` 和 `humi` 是兼容字段，会映射到内部温湿度
- `in_temp` 和 `in_humi` 是内部温湿度的标准字段
- `out_temp` 和 `out_humi` 是外部温湿度的标准字段
- 建议使用标准字段名以确保数据准确性

### 2.2 MQTT 消息格式
```json
{
  "deviceId": "pi5-vision-client",
  "timestamp": 1712000000000,
  "sensors": [
    { "type": "in_temp", "value": 35.2, "unit": "°C" },
    { "type": "in_humi", "value": 65.5, "unit": "%" },
    { "type": "out_temp", "value": 28.5, "unit": "°C" },
    { "type": "out_humi", "value": 75.0, "unit": "%" },
    { "type": "weight", "value": 45.8, "unit": "kg" },
    { "type": "in_count", "value": 120 },
    { "type": "out_count", "value": 115 },
    { "type": "hornet_count", "value": 2 },
    { "type": "lat", "value": 25.234489 },
    { "type": "lon", "value": 103.008597 }
  ],
  "status": {
    "online": true,
    "rssi": -45,
    "ip": "10.99.242.114"
  }
}
```

**兼容格式示例**（使用 temp/humi 作为内部温湿度）：
```json
{
  "deviceId": "pi5-vision-client",
  "timestamp": 1712000000000,
  "sensors": [
    { "type": "temp", "value": 35.2, "unit": "°C" },
    { "type": "humi", "value": 65.5, "unit": "%" },
    { "type": "weight", "value": 45.8, "unit": "kg" },
    { "type": "in_count", "value": 120 },
    { "type": "out_count", "value": 115 },
    { "type": "hornet_count", "value": 2 }
  ],
  "status": {
    "online": true,
    "rssi": -45,
    "ip": "10.99.242.114"
  }
}
```

## 三、数据库表结构

### 3.1 hive_data 表（蜂箱核心数据）
| 字段名 | 类型 | 对应树莓派字段 | 描述 |
|--------|------|---------------|------|
| `timestamp` | BIGINT | - | 时间戳（毫秒） |
| `temperature` | DOUBLE | temp | 蜂箱温度（兼容字段，优先使用内部温度） |
| `humidity` | DOUBLE | humi | 蜂箱湿度（兼容字段，优先使用内部湿度） |
| `insideTemperature` | DOUBLE | in_temp / temp | 蜂箱内部温度 |
| `insideHumidity` | DOUBLE | in_humi / humi | 蜂箱内部湿度 |
| `outsideTemperature` | DOUBLE | out_temp | 蜂箱外部温度 |
| `outsideHumidity` | DOUBLE | out_humi | 蜂箱外部湿度 |
| `weight` | DOUBLE | weight | 蜂箱重量 |
| `beesIn` | INT | in_count | 蜜蜂进入计数 |
| `beesOut` | INT | out_count | 蜜蜂出去计数 |
| `hornetsDetected` | INT | hornet_count | 胡蜂检测数量 |
| `latitude` | DOUBLE | lat | 纬度 |
| `longitude` | DOUBLE | lon | 经度 |

### 3.2 iot_telemetry 表（IoT遥测数据）
| 字段名 | 类型 | 描述 |
|--------|------|------|
| `id` | BIGINT | 自增主键 |
| `timestamp` | BIGINT | 时间戳（毫秒） |
| `device_id` | VARCHAR(64) | 设备ID |
| `sensor_type` | VARCHAR(64) | 传感器类型 |
| `value` | DOUBLE | 传感器值 |
| `unit` | VARCHAR(16) | 单位 |
| `qos` | INT | 服务质量等级 |

### 3.3 iot_device_status 表（设备状态）
| 字段名 | 类型 | 描述 |
|--------|------|------|
| `device_id` | VARCHAR(64) | 设备ID（主键） |
| `online` | BOOLEAN | 在线状态 |
| `last_seen_at` | BIGINT | 最后在线时间 |
| `last_rssi` | INT | 信号强度 |
| `last_ip` | VARCHAR(45) | IP地址 |
| `packets_received` | INT | 接收包数 |
| `packets_dropped` | INT | 丢包数 |

## 四、字段映射关系

### 4.1 树莓派 → 数据库映射
| 树莓派字段 | 数据库字段 | 前端字段 | 描述 |
|-----------|-----------|---------|------|
| `temp` | `insideTemperature` | `insideTemperature` | 蜂箱内部温度（°C）- 兼容字段 |
| `humi` | `insideHumidity` | `insideHumidity` | 蜂箱内部湿度（%）- 兼容字段 |
| `in_temp` | `insideTemperature` | `insideTemperature` | 蜂箱内部温度（°C） |
| `in_humi` | `insideHumidity` | `insideHumidity` | 蜂箱内部湿度（%） |
| `out_temp` | `outsideTemperature` | `outsideTemperature` | 蜂箱外部温度（°C） |
| `out_humi` | `outsideHumidity` | `outsideHumidity` | 蜂箱外部湿度（%） |
| `weight` | `weight` | `weight` | 蜂箱重量 |
| `in_count` | `beesIn` | `beesIn` | 蜜蜂进入计数 |
| `out_count` | `beesOut` | `beesOut` | 蜜蜂出去计数 |
| `hornet_count` | `hornetsDetected` | `hornetsDetected` | 胡蜂检测数量 |
| `lat` | `latitude` | `latitude` | 纬度 |
| `lon` | `longitude` | `longitude` | 经度 |
| `fps` | `iot_telemetry.fps` | `fps` | 视觉识别帧率 |
| `latency_ms` | `iot_telemetry.latency_ms` | `latency_ms` | 推理延迟 |

### 4.2 字段别名映射
后端支持多种字段别名，自动映射到标准字段：

```typescript
// 温度字段别名
temp → inside_temperature
temperature → inside_temperature
in_temp → inside_temperature
inside_temp → inside_temperature
out_temp → outside_temperature
outside_temp → outside_temperature
outside_temperature → outside_temperature

// 湿度字段别名
hum → inside_humidity
humi → inside_humidity
humidity → inside_humidity
in_humi → inside_humidity
inside_humi → inside_humidity
out_humi → outside_humidity
outside_humi → outside_humidity
outside_humidity → outside_humidity

// 重量字段别名
hive_weight → weight
weight_kg → weight

// 蜜蜂计数别名
beesin → bees_in
bee_in → bees_in
beesout → bees_out
bee_out → bees_out

// 胡蜂计数别名
hornets → hornet_count
hornet → hornet_count
hornets_detected → hornet_count
```

## 五、数据完整性保障

### 5.1 数据验证
- ✅ 字段类型验证（数字、字符串、布尔值）
- ✅ 数值范围验证（温度、湿度、重量等）
- ✅ 时间戳有效性验证
- ✅ 设备ID验证

### 5.2 数据存储策略
- **实时数据**: 通过 MQTT 实时上报，后端通过 SSE 推送到前端
- **历史数据**: 按时间桶聚合存储（默认60分钟一个桶）
- **IoT数据**: 存储到 `iot_telemetry` 表，支持多种传感器类型
- **蜂箱数据**: 存储到 `hive_data` 表，包含所有关键指标

### 5.3 数据一致性检查
系统提供以下API端点进行数据完整性检查：

#### 5.3.1 数据映射验证
```http
GET /api/system/data-mapping
Authorization: Bearer {token}
```

返回示例：
```json
{
  "timestamp": "2026-04-01T12:00:00.000Z",
  "raspberryPiFields": ["temp", "humi", "weight", ...],
  "databaseFields": ["temperature", "humidity", "weight", ...],
  "mappings": [
    {
      "source": "树莓派.temp",
      "target": "数据库.insideTemperature",
      "status": "mapped",
      "description": "蜂箱内部温度（°C）"
    }
  ],
  "coverage": {
    "raspberryPiToDatabase": 100,
    "databaseToFrontend": 100,
    "overall": 100
  },
  "issues": []
}
```

#### 5.3.2 数据样本验证
```http
GET /api/system/data-sample/{deviceId}
Authorization: Bearer {token}
```

返回示例：
```json
{
  "success": true,
  "message": "数据完整性验证通过",
  "sample": {
    "iot": [...],
    "hive": {...}
  },
  "issues": []
}
```

#### 5.3.3 数据一致性检查
```http
GET /api/system/consistency-check
Authorization: Bearer {token}
```

返回示例：
```json
{
  "timestamp": "2026-04-01T12:00:00.000Z",
  "duration": 125,
  "checks": [
    {
      "table": "hive_data",
      "checkType": "duplicate_timestamps",
      "status": "pass",
      "message": "无重复时间戳"
    }
  ],
  "summary": {
    "total": 12,
    "passed": 10,
    "warnings": 2,
    "errors": 0
  }
}
```

#### 5.3.4 字段映射文档
```http
GET /api/system/field-mapping-docs
Authorization: Bearer {token}
```

返回 Markdown 格式的字段映射文档。

## 六、前端数据展示

### 6.1 数据适配器
前端使用数据适配器 (`hiveDataAdapter.ts`) 处理数据字段：

```typescript
// 优先使用内部温度，回退到通用温度
resolveInsideTemperature(row) → insideTemperature ?? temperature

// 优先使用内部湿度，回退到通用湿度
resolveInsideHumidity(row) → insideHumidity ?? humidity

// 优先使用外部温度
resolveOutsideTemperature(row) → outsideTemperature

// 优先使用外部湿度
resolveOutsideHumidity(row) → outsideHumidity

// 主温度（内部优先，外部回退）
resolvePrimaryTemperature(row) → insideTemperature ?? outsideTemperature

// 主湿度（内部优先，外部回退）
resolvePrimaryHumidity(row) → insideHumidity ?? outsideHumidity
```

### 6.2 数据展示字段
| 前端展示 | 数据库字段 | 描述 |
|---------|-----------|------|
| 温度 | `insideTemperature` / `temperature` | 蜂箱温度 |
| 湿度 | `insideHumidity` / `humidity` | 蜂箱湿度 |
| 重量 | `weight` | 蜂箱重量 |
| 蜜蜂进入 | `beesIn` | 蜜蜂进入计数 |
| 蜜蜂出去 | `beesOut` | 蜜蜂出去计数 |
| 胡蜂检测 | `hornetsDetected` | 胡蜂检测数量 |
| 位置 | `latitude` / `longitude` | 蜂箱位置 |
| 地址 | `address` | 地理位置地址（通过高德API解析） |

## 七、数据质量保障

### 7.1 数据质量指标
- **新鲜度**: 数据更新时间距离当前时间的差值
- **完整性**: 数据字段的完整程度
- **准确性**: 数据值是否在合理范围内
- **一致性**: 数据在不同表之间的一致性

### 7.2 数据质量评估
前端实时显示数据质量评分：

```typescript
const dataQuality = {
  freshness: Math.max(0, 100 - (dataAge / 10000) * 100), // 10秒内为100%
  completeness: Math.min(100, (dataCount / 10) * 100), // 10个数据点为100%
  overall: Math.round((freshness + completeness) / 2)
};
```

### 7.3 数据异常检测
系统自动检测以下异常情况：
- ❌ 温度超出范围（-20°C ~ 60°C）
- ❌ 湿度超出范围（0% ~ 100%）
- ❌ 重量为负数
- ❌ 胡蜂检测数量异常
- ⚠️ 数据时间间隔过大（超过2小时）
- ⚠️ 数据重复记录

## 八、故障排查指南

### 8.1 数据未显示
1. 检查树莓派是否正常上报数据
2. 检查MQTT连接状态
3. 检查数据库连接状态
4. 检查SSE连接状态
5. 使用数据映射验证API检查字段映射

### 8.2 数据不完整
1. 使用数据样本验证API检查数据完整性
2. 检查传感器是否正常工作
3. 检查数据上报频率
4. 检查数据库存储策略

### 8.3 数据不一致
1. 使用数据一致性检查API
2. 检查字段映射关系
3. 检查数据类型转换
4. 检查时区设置

## 九、最佳实践

### 9.1 数据上报建议
- 📊 上报频率：建议3秒一次（`publish_interval_seconds: 3.0`）
- 📊 数据格式：使用标准JSON格式
- 📊 字段命名：使用标准字段名或别名
- 📊 时间戳：使用毫秒级Unix时间戳

### 9.2 数据存储建议
- 💾 定期备份数据库
- 💾 监控数据库存储空间
- 💾 定期清理历史数据（保留最近6个月）
- 💾 优化数据库索引

### 9.3 数据展示建议
- 📈 使用实时数据更新（SSE）
- 📈 提供历史数据查询
- 📈 显示数据质量指标
- 📈 异常数据高亮显示

## 十、总结

通过以上方案，系统实现了：

✅ **100%字段映射**: 所有树莓派上传的字段都能正确映射到数据库和前端
✅ **数据完整性保障**: 通过多重验证确保数据质量
✅ **实时数据同步**: 通过SSE实现秒级数据更新
✅ **历史数据存储**: 按时间桶聚合存储，节省存储空间
✅ **数据一致性检查**: 提供多种API端点进行数据验证
✅ **故障快速定位**: 提供详细的故障排查指南

系统现在可以确保树莓派采集的数据100%对应匹配后端数据库和前端展示！

补充：数据生命周期治理已新增“过时数据清理”闭环，支持按表提取过时数据、生成AI结构化报告、人工确认后执行备份清理，并在 `stale_cleanup_operations` 记录全流程审计信息。
