# 智能蜂箱数据同步与一致性方案

更新时间：2026-04-05  
适用版本：当前仓库 `services/dataSyncService.ts` + `services/mqttIngestService.ts` + `server.ts` + `hornet_model2_ncnn_model/config.py`

## 一、方案概述

本方案实现了前端与后端数据的精确对应和实时同步，采用树莓派通过 MQTT 上报数据到后端、后端通过 SSE 实时推送到前端，并在后端按时间桶（默认 60 分钟）聚合落库。
同时提供“断网容灾”：设备在网络中断时依旧按小时采样落盘（SQLite），网络恢复后通过 MQTT 补传，成功后清理缓存，确保空间充足。

## 二、核心功能

### 1. 实时数据同步（1秒内）

- **同步方式**：
  - MQTT消息推送（树莓派 → 后端）
  - SSE实时转发（后端 → 前端）
  - 数据变化即时触发更新
- **更新机制**：
  - 树莓派按 `mqtt.publish_interval_seconds`（建议 3 秒）采集并通过 MQTT 发送
  - 后端接收后立即通过SSE推送到前端
  - 前端实时显示最新数据
- **性能优化**：
  - 直接推送减少中间环节
  - 增量更新减少数据传输
  - 后台处理不影响用户体验

### 2. 数据存储机制

- **存储策略**：后端按时间桶聚合后落库（默认 60 分钟一个桶）
- **数据聚合**：
  - 计算每小时平均值（内部温度、内部湿度、外部温度、外部湿度等）
  - 计算每小时累计值（马蜂检测、蜜蜂进出等）
  - 确保数据代表性和存储效率
- **传感器支持**：
  - 双DHT22传感器配置（内部和外部温湿度）
  - 内部传感器：`in_temp`, `in_humi` → `insideTemperature`, `insideHumidity`
  - 外部传感器：`out_temp`, `out_humi` → `outsideTemperature`, `outsideHumidity`
  - 兼容字段：`temp`, `humi` → 自动映射到内部温湿度
- **存储流程**：
  1. 树莓派实时采集数据（支持双传感器）
  2. 数据通过MQTT发送到后端
  3. 后端缓存数据并实时推送到前端
  4. 后端按"消息 timestamp 所在桶"聚合并存储到数据库
  5. 断网补传场景：补传数据携带 `status.replay=true`，后端依旧聚合落库，但会跳过实时推送（避免刷屏）且不更新设备在线状态（避免覆盖 `lastSeenAt`）

### 3. 数据校验机制

#### 3.1 校验和验证
- 使用哈希算法计算数据校验和
- 对比前后数据校验和检测变化
- 关键数据点抽样验证

#### 3.2 数据一致性检查
- 数据长度对比
- 时间戳连续性检查
- 数值范围合理性验证
- 必填字段完整性检查

### 4. 缓存策略

#### 4.1 多级缓存
- **前端缓存**：内存缓存（DataSyncService）
- **后端缓存**：每小时数据缓存（hourlyCache）
- **持久化缓存**：数据库存储

#### 4.2 缓存策略
- **前端缓存**：TTL 60秒，定期清理过期数据
- **后端缓存**：按时间桶聚合缓存（默认 60 分钟/桶），落库成功后清理对应桶
- **离线处理**：设备端（树莓派）在网络中断时将“小时采样记录”落盘到 SQLite，网络恢复后 MQTT 补传并清理本地缓存

### 5. 重试机制

#### 5.1 自动重试
- **MQTT连接**：自动重连，最多3次，指数退避
- **WebSocket连接**：自动重连，通知前端
- **数据存储**：失败后重试，确保数据不丢失
- **数据获取**：失败后自动重试，最多3次

#### 5.2 离线处理
- **离线检测**：自动检测网络状态
- **离线缓存**：本地存储未发送的数据
- **恢复同步**：网络恢复后自动重新同步

## 三、技术实现

### 1. 核心服务

#### 1.1 MqttIngestService（MQTT数据接收服务）
- **主要功能**：
  - `startMqttIngestService()` - 启动MQTT服务
  - `parseMqttPayload()` - 解析MQTT消息
  - 实时推送数据到前端（SSE）
  - 每小时存储聚合数据到数据库
- **实现细节**：
  - 使用MQTT协议接收树莓派发送的数据
  - 通过realtimeHub广播数据到前端（SSE）
  - 缓存数据用于每小时聚合
  - 按“timestamp 所在时间桶”聚合并落库，定时任务只负责扫描“已结束桶”
  - 对 `status.replay=true` 的补传数据：只落库，不做实时广播，不更新设备在线状态

#### 1.2 DataSyncService（数据同步服务）
- **主要功能**：
  - `subscribe<T>(key, callback, options)` - 订阅数据
  - `forceSync(key)` - 强制同步
  - `getSyncStatus()` - 获取同步状态
  - `clearCache()` - 清除缓存
- **实现细节**：
  - 内存缓存，TTL 60秒
  - 网络状态监听，支持离线模式
  - 带重试的数据获取，最多3次
  - 数据一致性验证，使用哈希算法

#### 1.3 前端Hook
- **useDataSync**：通用数据同步Hook
  ```typescript
  const { data, isLoading, error, lastSyncTime, forceRefresh } = useDataSync(
    'live-hive-data',
    () => fetchHiveData(),
    {
      interval: 3000, // 3秒刷新
      onError: (message) => console.error(message)
    }
  );
  ```

### 2. 数据流

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  树莓派      │────▶│  MQTT Broker │────▶│  后端服务    │────▶│  前端界面    │
└─────────────┘     └─────────────┘     └─────────────┘     └─────────────┘
                                      │
                                      ▼
                                ┌─────────────┐
                                │  数据库     │
                                └─────────────┘
                                      │
                                      │ 每小时存储
                                      ▼
                                ┌─────────────┐
                                │  聚合数据    │
                                └─────────────┘

补充：断网容灾（设备端）

```
树莓派(实时 MQTT 3s)  ───────────────▶  后端(SSE) ▶ 前端
树莓派(小时采样 SQLite) ──断网堆积──▶  网络恢复后 MQTT 补传(QoS1) ▶ 后端按桶落库
```
```

### 3. 错误处理

#### 3.1 错误类型
- **网络错误**：自动重试，使用缓存数据
- **MQTT连接错误**：自动重连，记录日志
- **WebSocket错误**：自动重连，通知前端
- **数据验证错误**：触发告警，拒绝更新
- **一致性错误**：自动重新获取数据

#### 3.2 错误提示
- **前端状态指示器**：显示同步状态和错误信息
- **组件级错误处理**：使用try-catch处理回调错误
- **用户友好提示**：中文错误信息
- **后端日志**：详细的错误日志和处理情况

## 四、使用方式

### 1. 数据同步服务

```typescript
import { dataSyncService, useDataSync } from '../services/dataSyncService';

// 使用Hook
function HiveDataComponent() {
  const { data, isLoading, error, lastSyncTime, forceRefresh } = useDataSync(
    'live-hive-data',
    () => fetchHiveData(),
    {
      interval: 3000, // 3秒刷新
      onError: (message) => console.error(message)
    }
  );
  
  if (isLoading) return <div>加载中...</div>;
  if (error) return <div>错误: {error}</div>;
  
  return (
    <div>
      <div>最后同步: {new Date(lastSyncTime).toLocaleString()}</div>
      <div>温度: {data?.temperature}°C</div>
      <div>湿度: {data?.humidity}%</div>
      <button onClick={forceRefresh}>刷新数据</button>
    </div>
  );
}

// 直接使用服务
function App() {
  useEffect(() => {
    const unsubscribe = dataSyncService.subscribe('live-hive-data', (data) => {
      if ('_error' in data) {
        console.error('同步错误:', data._message);
      } else {
        console.log('数据更新:', data);
      }
    }, {
      fetchFn: () => fetchHiveData(),
      interval: 3000
    });
    
    return unsubscribe;
  }, []);
  
  return <HiveDataComponent />;
}
```

### 2. 实时IoT数据

```typescript
import { useIotRealtime } from '../hooks/useIotRealtime';

function IotRealtimeComponent() {
  const { 
    latest, 
    history, 
    streamConnected, 
    lastUpdated, 
    dataQuality 
  } = useIotRealtime(
    'pi5-vision-client', 
    600000, // 10分钟数据范围
    {
      onStreamStatusChange: (connected) => {
        console.log('Stream status:', connected);
      },
      onDataUpdate: (data) => {
        console.log('Data updated:', data.length);
      }
    }
  );
  
  return (
    <div>
      <div>连接状态: {streamConnected ? '在线' : '离线'}</div>
      <div>最后更新: {lastUpdated ? new Date(lastUpdated).toLocaleString() : '从未'}</div>
      <div>数据质量: {dataQuality.overall}%</div>
      <div>最新数据: {latest.length} 条</div>
    </div>
  );
}
```

### 3. 获取同步状态

```typescript
function SyncStatusComponent() {
  const [status, setStatus] = useState(dataSyncService.getSyncStatus());
  
  useEffect(() => {
    const interval = setInterval(() => {
      setStatus(dataSyncService.getSyncStatus());
    }, 5000);
    
    return () => clearInterval(interval);
  }, []);
  
  return (
    <div>
      <div>网络状态: {status.isOnline ? '在线' : '离线'}</div>
      <div>缓存大小: {status.cacheSize}</div>
      <div>订阅数: {status.subscriberCount}</div>
      <div>最后同步: {Object.entries(status.lastSyncTimes).map(([key, time]) => (
        <div key={key}>{key}: {new Date(time).toLocaleString()}</div>
      ))}</div>
    </div>
  );
}
```

## 五、配置参数

### 5.1 数据同步配置
```typescript
const SYNC_CONFIG = {
  // 实时更新间隔（毫秒）- 默认3秒
  REALTIME_INTERVAL: 3000,
  
  // 最大重试次数
  MAX_RETRY_COUNT: 3,
  
  // 重试延迟（毫秒）
  RETRY_DELAY: 1000,
  
  // 缓存有效期（毫秒）- 默认60秒
  CACHE_TTL: 60000,
  
  // 数据校验间隔（毫秒）- 默认10秒
  VALIDATION_INTERVAL: 10000,
};
```

### 5.2 MQTT配置
```typescript
// 环境变量配置
MQTT_BROKER_URL=mqtt://127.0.0.1:1883
MQTT_SENSOR_TOPIC=smarthive/+/sensors
MQTT_SUB_QOS=1
MQTT_CLIENT_ID=smarthive-server-${process.pid}
MQTT_USERNAME=your_username
MQTT_PASSWORD=your_password
```

## 六、监控与调试

### 1. 控制台日志
```
[MQTT] Received data from pi5-vision-client, cached for hourly storage
[DataSync] 数据同步完成: live-hive-data, 耗时: 245ms
[DataSync] 网络已中断，切换到离线模式...
[DataSync] 网络已恢复，重新同步数据...
[MQTT] Hourly data stored for device pi5-vision-client
```

### 2. 性能指标
- **同步延迟**：< 1秒
- **数据准确性**：100%
- **缓存命中率**：> 80%
- **错误恢复时间**：< 5秒
- **存储效率**：每小时存储一次，减少数据库压力

### 3. 监控面板
- **MQTT连接状态**：在线/离线
- **SSE连接状态**：连接/断开
- **数据接收统计**：消息数、错误数
- **存储状态**：存储次数、成功/失败
- **同步状态**：在线/离线、缓存大小、订阅数

## 七、最佳实践

### 1. 数据订阅
- 使用 `useDataSync` 获取通用数据
- 使用 `useIotRealtime` 获取实时IoT数据
- 合理设置同步间隔，避免频繁请求

### 2. 错误处理
- 始终处理 `error` 状态
- 提供用户友好的错误提示
- 监控网络状态变化
- 合理使用缓存数据

### 3. 性能优化
- 合理设置数据范围和刷新频率
- 避免不必要的重新渲染
- 优化数据处理逻辑
- 定期清理过期缓存

### 4. 部署建议
- 确保MQTT broker稳定运行
- 配置适当的网络带宽
- 定期备份数据库
- 监控系统资源使用

## 八、注意事项

1. **网络不稳定**：系统会自动切换到离线模式，使用缓存数据
2. **数据量大**：每小时存储聚合数据，减少存储压力
3. **并发更新**：系统会自动处理并发更新，确保数据一致性
4. **内存管理**：定期清理过期缓存，避免内存泄漏
5. **安全性**：确保MQTT和WebSocket连接的安全性

## 九、后续优化

1. **增量同步**：只传输变化的数据，减少网络流量
2. **智能预加载**：根据用户行为预加载可能需要的数据
3. **数据压缩**：传输前压缩数据，提高传输效率
4. **多端同步**：支持多个客户端之间的数据同步
5. **边缘计算**：在树莓派端进行初步数据处理，减少传输量
6. **预测分析**：基于历史数据进行预测分析，提供智能建议

## 十、与过时数据治理协同（新增）

1. 实时同步链路（MQTT/SSE）负责“数据进入系统”的可靠性与一致性。  
2. 过时数据治理链路负责“数据生命周期末端处理”，接口为：
   - `POST /api/system/stale-data/report`
   - `POST /api/system/stale-data/cleanup`
   - `GET /api/system/stale-data/operation/:operationId`
3. 治理链路执行前必须报告确认与备份，执行后可审计追溯，不影响实时采集通道。

## 十一、ESP8266 数据链路验证（新增）

**验证结果**: ✅ 通过

**验证内容**:
1. **数据链路**: ESP8266 → MQTT → 后端 → MySQL 测试库
2. **测试数据库**: `smarthive_esp_test`
3. **MQTT Topic**: `smarthive/esp8266-test/sensors`
4. **支持字段**: `in_temp`、`in_humi`（映射为内部温湿度）

**验证步骤**:
1. 执行 `sql/esp8266_test_db_setup.sql` 创建测试库
2. 修改 `.env` 中的 `DB_NAME=smarthive_esp_test`
3. 烧录 ESP8266 示例程序（`hardware/esp8266_mqtt_dht/esp8266_mqtt_dht.ino`）
4. 检查 ESP8266 串口监视器显示 `MQTT publish OK`
5. 验证数据入库: `SELECT * FROM iot_telemetry WHERE device_id='esp8266-test' ORDER BY id DESC LIMIT 10;`
6. 验证后端接口: `GET /api/iot/monitor` 确认 `receivedMessages` 增长

**技术实现**:
- **MQTT 接收**: 使用 `services/mqttIngestService.ts` 中的 `startMqttIngestService()` 服务
- **字段映射**: 使用 `services/iotBridge.ts` 中的 `SENSOR_ALIAS_MAP` 进行字段映射
- **数据存储**: 使用 `services/databaseService.ts` 中的 `insertIotTelemetry()` 方法
- **实时推送**: 使用 `services/realtimeHub.ts` 通过 SSE 推送到前端

**意义**:
1. **验证了数据链路的完整性**: ESP8266 到后端的 MQTT 数据链路已成功验证
2. **为树莓派部署奠定基础**: 验证了字段映射、数据存储和实时推送的正确性
3. **提高了系统可靠性**: 验证了系统在不同设备类型下的兼容性
4. **简化了部署流程**: 提供了完整的测试方案，降低了树莓派部署的风险

**结论**:
ESP8266 数据链路的成功验证，证明了系统的数据同步与一致性机制的可靠性和灵活性，为后续树莓派的部署和集成提供了有力保障。

## 十二、新硬件设备数据链路（新增）

### 12.1 加湿器模块数据链路

**设备类型**: 执行器（输出设备）  
**接口**: GPIO25（低电平触发）  
**数据流向**: 后端AI检测 → 树莓派GPIO控制 → 继电器动作 → 加湿器启动

**工作流程**:
```
摄像头采集 → NCNN推理 → 胡蜂检测结果
    ↓
vision_process.py 发送信号
    ↓
sensor_process.py 接收信号 → GPIO25拉低(30秒)
    ↓
继电器模块吸合 → 5V电源接通 → 加湿器启动
    ↓
30秒后自动关闭（防止过度加湿）
```

**配置参数**:
```python
# config.py - 加湿器配置
humidifier_enabled: bool = True           # 启用状态
humidifier_gpio_pin: int = 25             # GPIO引脚
humidifier_active_low: bool = True        # 低电平触发
humidifier_trigger_duration_ms: int = 30000  # 触发持续时间
```

**安全机制**:
1. **定时关闭**: 防止持续运行导致过湿（>85%）
2. **冷却期**: 避免频繁触发（建议5分钟冷却期）
3. **日志记录**: 记录每次触发时间、持续时间、原因

### 12.2 OLED显示屏数据链路

**设备类型**: 显示设备（输出设备）  
**接口**: I2C (SDA:GPIO2, SCL:GPIO3)  
**驱动芯片**: SSD1306  
**分辨率**: 128×64像素  
**刷新频率**: 1Hz (每秒更新一次)

**数据流向**:
```
DHT22传感器读取 → SharedSensorState → OLED显示模块
    ↓
sensor_process.py 更新传感器状态
    ↓
oled_display_service.py 读取最新数据
    ↓
I2C总线传输 → SSD1306驱动 → 屏幕显示
```

**显示内容**:
- **主标题**: SmartHive Monitor
- **内部温湿度**: IN: XX.X°C / XX.X%
- **外部温湿度**: OUT: XX.X°C / XX.X%
- **重量** (必需): Weight: XX.Xkg
- **胡蜂数量** (必需): Hornets: X
- **系统状态**: ONLINE/OFFLINE/ERROR

**配置参数**:
```python
# config.py - OLED配置
oled_enabled: bool = True                    # 启用状态
oled_i2c_address: str = "0x3C"              # I2C地址
oled_width: int = 128                        # 屏幕宽度
oled_height: int = 64                        # 屏幕高度
oled_refresh_interval: float = 1.0           # 刷新间隔（秒）
```

**技术实现要点**:
1. **双缓冲显示**: 避免屏幕闪烁
2. **I2C通信**: 使用 `adafruit-circuitpython-ssd1306` 库
3. **字体渲染**: 使用Pillow库进行文字渲染
4. **错误处理**: I2C通信失败时自动重试

### 12.3 完整数据流架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                     感知层 (输入)                                    │
│  ┌──────────┐ ┌──────────┐ ┌───────┐ ┌──────┐ ┌──────┐           │
│  │ DHT22×2  │ │ HX711    │ │ IR×2  │ │ GPS  │ │Camera│           │
│  │(GPIO4,17)│ │(GPIO5,6) │ │(23,24)│ │(UART)│ │(CSI) │           │
│  └────┬─────┘ └────┬─────┘ └──┬───┘ └──┬───┘ └──┬───┘           │
│       └────────────┴────────────┴────────┴────────┘                │
│                            ↓                                       │
│              ┌─────────────────────────┐                           │
│              │   SharedSensorState     │                           │
│              │   (数据聚合)            │                           │
│              └────────────┬────────────┘                           │
├───────────────────────────┼───────────────────────────────────────┤
│                           ↓                                         │
│              ┌─────────────────────────┐                           │
│              │   MQTT Publisher        │                           │
│              │   (数据上报)            │                           │
│              └────────────┬────────────┘                           │
├───────────────────────────┼───────────────────────────────────────┤
│                           ↓                                         │
│              ┌─────────────────────────┐                           │
│              │   MQTT Broker          │                           │
│              │   (消息队列)            │                           │
│              └────────────┬────────────┘                           │
├───────────────────────────┼───────────────────────────────────────┤
│                    执行层 (输出设备)                                 │
│  ┌──────────────────┐  ┌──────────────────┐  ┌────────────────┐   │
│  │   舵机云台       │  │   加湿器模块     │  │  OLED显示屏    │   │
│  │   (GPIO18/PWM)  │  │   (GPIO25/继电器)│  │  (I2C/SSD1306) │   │
│  │   摄像头扫描     │  │   胡蜂驱赶(5V)  │  │  实时温湿度    │   │
│  └──────────────────┘  └──────────────────┘  └────────────────┘   │
└───────────────────────────┴───────────────────────────────────────┘
```

### 12.4 数据流详细说明
├───────────────────────────────────────────────────────────┤
│                       执行层 (输出)                          │
│  ┌──────────────┐  ┌──────────────┐                       │
│  │ 加湿器模块    │  │ OLED显示屏   │                       │
│  │ (GPIO25)     │  │ (I2C:SDA/SCL)│                       │
│  │ 继电器控制    │  │ SSD1306驱动  │                       │
│  └──────────────┘  └──────────────┘                       │
└───────────────────────────────────────────────────────────┘
```

### 12.4 数据一致性保障

**感知层到执行层的一致性**:

1. **温湿度数据一致性**:
   - DHT22读取 → MQTT上报 → MySQL存储 → SSE推送 → 前端显示 → OLED显示
   - 所有环节使用相同字段映射规则 (`in_temp` → `insideTemperature`)

2. **胡蜂检测到执行的一致性**:
   - 摄像头捕获 → NCNN推理 → 检测结果 → GPIO25触发 → 继电器动作
   - 整个过程在树莓派本地完成，延迟<100ms

3. **时间戳同步**:
   - 所有传感器数据使用统一的时间戳（毫秒级）
   - NTP同步确保树莓派时间准确
   - 数据库存储使用UTC时间，前端显示转换为本地时间

**异常处理机制**:

1. **传感器故障**: 自动使用上一次有效值，记录错误日志
2. **MQTT断连**: 本地缓存数据，恢复后补传
3. **GPIO控制失败**: 重试3次，失败后发送告警
4. **I2C通信失败**: 自动重连，降级显示（仅显示状态）
5. **数据库写入失败**: 缓存到本地SQLite，定期重试

### 12.5 性能指标

| 指标 | 数值 | 说明 |
|------|------|------|
| 传感器读取频率 | 0.5-5秒 | 可配置 |
| MQTT发布间隔 | 2-3秒 | 可配置 |
| 数据库落库周期 | 60分钟 | 时间桶聚合 |
| SSE推送延迟 | <1秒 | 实时性 |
| OLED刷新频率 | 1Hz | 平衡性能与功耗 |
| 加湿器响应时间 | <100ms | 本地GPIO控制 |
| 系统整体延迟 | <2秒 | 从传感器到显示 |

**结论**:
新硬件设备的加入完善了系统的执行层能力，实现了从感知到决策再到执行的完整闭环。加湿器模块和OLED显示屏的配置已集成到系统中，确保了数据的一致性和系统的可靠性。

