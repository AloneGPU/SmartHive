# 智能蜂箱系统部署指南

文档版本：2026-04-05  
适用环境：Linux服务器（CentOS/Ubuntu）

---

## 一、部署概述

### 1.1 系统架构

```
┌─────────────┐     MQTT      ┌─────────────┐     SSE      ┌─────────────┐
│   树莓派     │ ──────────> │   后端服务   │ <─────────> │   前端页面   │
│  (传感器)    │            │  (Node.js)   │            │  (React)    │
└─────────────┘            └─────────────┘            └─────────────┘
                                  │
                                  │ 存储数据
                                  ▼
                           ┌─────────────┐
                           │   MySQL     │
                           │  (数据库)   │
                           └─────────────┘
```

### 1.2 软件环境要求

| 软件 | 版本要求 | 说明 |
|------|---------|------|
| Node.js | 20.x LTS | 后端运行环境 |
| MySQL | 8.0+ | 数据存储 |
| Nginx | 1.20+ | 反向代理 |
| PM2 | 全局安装 | 进程管理 |
| MQTT Broker | Mosquitto 2.0+ | 必需，用于树莓派数据采集 |

### 1.3 硬件要求

| 配置项 | 最低要求 | 推荐配置 |
|--------|---------|---------|
| CPU | 2核 | 4核+ |
| 内存 | 2GB | 4GB+ |
| 存储 | 20GB | 50GB+ SSD |
| 带宽 | 1Mbps | 10Mbps+ |

---

## 二、服务器准备

### 2.1 安装 Node.js

```bash
# CentOS
curl -fsSL https://rpm.nodesource.com/setup_20.x | sudo bash -
sudo yum install -y nodejs

# Ubuntu
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# 验证安装
node --version
npm --version
```

### 2.2 安装 PM2

```bash
sudo npm install -g pm2

# 验证安装
pm2 --version
```

### 2.3 安装 Nginx

```bash
# CentOS
sudo yum install -y nginx

# Ubuntu
sudo apt update
sudo apt install -y nginx

# 启动并设置开机自启
sudo systemctl start nginx
sudo systemctl enable nginx
```

### 2.4 安装 MySQL

```bash
# CentOS
sudo yum install -y mysql-server
sudo systemctl start mysqld
sudo systemctl enable mysqld

# Ubuntu
sudo apt install -y mysql-server
sudo systemctl start mysql
sudo systemctl enable mysql

# 安全配置
sudo mysql_secure_installation
```

### 2.5 安装 MQTT Broker（必需）

```bash
# CentOS
sudo yum install -y mosquitto mosquitto-clients

# Ubuntu
sudo apt install -y mosquitto mosquitto-clients

# 启动服务
sudo systemctl start mosquitto
sudo systemctl enable mosquitto
```

---

## 三、数据库配置

### 3.1 创建数据库和用户

```sql
-- 登录 MySQL
mysql -u root -p

-- 创建数据库
CREATE DATABASE smarthive CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建用户
CREATE USER 'smarthive'@'localhost' IDENTIFIED BY '你的安全密码';

-- 授权
GRANT ALL PRIVILEGES ON smarthive.* TO 'smarthive'@'localhost';
FLUSH PRIVILEGES;

-- 退出
EXIT;
```

### 3.2 导入数据表结构

```bash
# 上传 create_tables.sql 到服务器后执行
mysql -u smarthive -p smarthive < create_tables.sql

# 验证表创建
mysql -u smarthive -p smarthive -e "SHOW TABLES;"
```

### 3.3 导入演示数据（可选）

```bash
mysql -u smarthive -p smarthive < smarthive_demo_seed.sql
```

---

## 四、应用部署

### 4.1 上传文件

将以下文件上传到服务器 `/www/wwwroot/smarthive/`：

```
dist/                  # 前端构建产物
dist-server/           # 后端构建产物
package.json           # 依赖配置
package-lock.json      # 依赖锁定
ecosystem.config.js    # PM2配置
.env.example           # 环境变量模板
```

### 4.2 配置环境变量

```bash
cd /www/wwwroot/smarthive

# 复制模板
cp .env.example .env

# 编辑配置
vim .env
```

**生产环境配置示例**：

```env
# 后端服务器配置
PORT=3001
NODE_ENV=production

# 安全配置 - 必须修改！
API_TOKEN=请生成一个至少16位的随机字符串

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=smarthive
DB_PASSWORD=你的数据库密码
DB_NAME=smarthive

# AI服务配置（可选）
QWEN_API_KEY=你的通义千问API密钥

# 地图服务配置（可选）
GAODE_API_KEY=你的高德地图API密钥

# CORS配置
CORS_ORIGIN=https://你的域名

# 视频流配置
VIDEO_STREAM_URL=/api/vision/stream.mjpg
VIDEO_STREAM_MODE=mjpeg
VISION_DEVICE_ID=pi5-vision-client

# MQTT配置（可选）
MQTT_BROKER_URL=mqtt://127.0.0.1:1883
MQTT_SENSOR_TOPIC=smarthive/+/sensors
MQTT_SUB_QOS=1
MQTT_STORAGE_BUCKET_MINUTES=60
IOT_MIRROR_TO_BEEHIVE=true
```

### 4.3 安装依赖

```bash
cd /www/wwwroot/smarthive
npm ci --production
```

### 4.4 启动后端服务

推荐使用构建后的单文件运行，性能更优且依赖更少：

```bash
# 使用 PM2 启动构建产物
pm2 start dist-server/server.cjs --name smarthive-backend

# 或者使用开发模式（仅建议调试用）
# npm run dev:server
```

### 4.5 验证后端服务

```bash
# 查看服务状态
pm2 status

# 查看日志
pm2 logs smarthive-backend --lines 50

# 测试健康检查
curl http://localhost:3001/api/health
```

预期响应：
```json
{
  "status": "ok",
  "databaseConnected": true,
  "timestamp": 1712000000000,
  "warnings": []
}
```

---

## 五、Nginx 配置

### 5.1 创建站点配置

创建 `/etc/nginx/conf.d/smarthive.conf`：

```nginx
server {
    listen 80;
    server_name 你的域名或IP;
    
    # 前端静态文件根目录
    root /www/wwwroot/smarthive/dist;
    index index.html;
    
    # 客户端请求体大小限制
    client_max_body_size 4m;
    
    # 前端路由 - 关键配置
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # SSE实时通道（关闭缓冲，避免延迟堆积）
    location = /api/iot/stream {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Connection '';
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
        add_header X-Accel-Buffering no;
    }

    # MJPEG实时流（关闭缓冲，避免画面卡顿）
    location = /api/vision/stream.mjpg {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Connection '';
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
        add_header X-Accel-Buffering no;
    }

    # 后端API反向代理
    location /api {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
    }
    
    # 静态文件缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 30d;
        add_header Cache-Control "public, max-age=2592000";
    }
    
    # 禁止访问敏感文件
    location ~* (\.env.*|\.git.*|\.gitignore|package\.json|package-lock\.json|node_modules) {
        deny all;
        return 404;
    }

    # 日志配置
    access_log /var/log/nginx/smarthive.access.log;
    error_log /var/log/nginx/smarthive.error.log;
}
```

### 5.2 测试并重载配置

```bash
# 测试配置语法
sudo nginx -t

# 重载配置
sudo nginx -s reload
```

### 5.3 配置 HTTPS（推荐）

```bash
# 安装 certbot
# CentOS
sudo yum install -y certbot python3-certbot-nginx

# Ubuntu
sudo apt install -y certbot python3-certbot-nginx

# 申请证书
sudo certbot --nginx -d 你的域名

# 测试自动续期
sudo certbot renew --dry-run

# 设置自动续期
sudo systemctl enable certbot-renew.timer
```

---

## 六、防火墙配置

### 6.1 CentOS (firewalld)

```bash
# 开放必要端口
sudo firewall-cmd --permanent --add-port=80/tcp
sudo firewall-cmd --permanent --add-port=443/tcp
sudo firewall-cmd --permanent --add-port=1883/tcp  # MQTT（必需）

# 重载防火墙
sudo firewall-cmd --reload

# 查看开放端口
sudo firewall-cmd --list-ports
```

### 6.2 Ubuntu (ufw)

```bash
# 开放必要端口
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 1883/tcp  # MQTT

# 启用防火墙
sudo ufw enable

# 查看状态
sudo ufw status
```

---

## 七、树莓派配置

### 7.1 配置文件

编辑 `hornet_model2_ncnn_model/config.yaml`：

```yaml
mqtt:
  enabled: true
  host: "你的服务器IP或域名"
  port: 1883
  data_topic: "smarthive/pi5-vision-client/sensors"
  client_id: "pi5-vision-client"
  publish_interval_seconds: 3.0
  publish_qos: 1
  compress_payload: true

server_upload:
  enabled: true
  url: "https://你的域名/api/vision/frame"
  token: "你的API_TOKEN"
  device_id: "pi5-vision-client"
  interval_seconds: 0.5
  batch_size: 10
```

### 7.2 启动树莓派程序

```bash
cd hornet_model2_ncnn_model
python3 tast.py --config config.yaml
```

---

## 八、部署验证

### 8.1 后端健康检查

```bash
curl https://你的域名/api/health
```

### 8.2 数据库连接验证

```bash
curl -H "Authorization: Bearer 你的API_TOKEN" \
  https://你的域名/api/beehive/latest
```

### 8.3 AI功能验证

```bash
curl -H "Authorization: Bearer 你的API_TOKEN" \
  https://你的域名/api/ai/database-schema
```

### 8.4 前端访问验证

1. 打开浏览器访问 `https://你的域名`
2. 点击"普通用户"登录
3. 检查总览页面是否显示数据
4. 进入"AI助手"测试AI对话

### 8.5 管理后台验证

1. 点击"管理员"登录
2. 输入密码 `admin123`（生产环境请修改）
3. 检查配置页面是否正常
4. 测试"视频探测"功能
5. **ESP8266 联调验证**：
   - 确保 `DB_NAME=smarthive_esp_test`（测试阶段）
   - 检查 ESP8266 串口监视器显示 `MQTT publish OK`
   - 执行 SQL：`USE smarthive_esp_test; SELECT * FROM iot_telemetry WHERE device_id='esp8266-test' ORDER BY id DESC LIMIT 10;`
   - 调用接口：`GET /api/iot/monitor` 确认 `receivedMessages` 增长
6. **树莓派 MQTT 入库验证**（生产阶段）：
   - 改回生产库：`DB_NAME=smarthive`
   - 通过接口 `GET /api/iot/monitor` 确认 `receivedMessages` 正在增长
   - 在生产库查询 `iot_telemetry` 中 `device_id='pi5-vision-client'` 的最新记录
7. **新硬件设备验证**（树莓派端）：
   - **加湿器模块测试**：手动触发GPIO25，验证继电器吸合和加湿器启动
   - **OLED显示屏测试**：检查I2C地址检测（`i2cdetect -y 1`），确认显示内外温湿度数据
   - **胡蜂驱赶联动测试**：模拟胡蜂检测信号，验证加湿器自动触发
8. 生成过时数据治理报告并确认能返回任务ID
9. 输入 `CONFIRM_CLEANUP` 后执行清理并检查备份路径
10. 查询 `/api/system/stale-data/operation/:operationId` 确认任务状态可追溯

---

## 九、运维命令

### 9.1 服务管理

```bash
# 查看服务状态
pm2 status

# 重启服务
pm2 restart smarthive-backend

# 停止服务
pm2 stop smarthive-backend

# 查看实时日志
pm2 logs smarthive-backend

# 查看最近100行日志
pm2 logs smarthive-backend --lines 100

# 监控面板
pm2 monit
```

### 9.2 日志查看

```bash
# 后端日志
pm2 logs smarthive-backend

# Nginx 访问日志
tail -f /var/log/nginx/smarthive.access.log

# Nginx 错误日志
tail -f /var/log/nginx/smarthive.error.log

# MySQL 日志
tail -f /var/log/mysqld.log

# MQTT 日志
tail -f /var/log/mosquitto/mosquitto.log
```

### 9.3 数据备份

```bash
# 创建备份目录
mkdir -p /backup/smarthive

# 备份数据库
mysqldump -u smarthive -p smarthive > /backup/smarthive/smarthive_$(date +%Y%m%d_%H%M%S).sql

# 设置定时备份（可选）
crontab -e
# 添加每天凌晨2点备份
0 2 * * * mysqldump -u smarthive -p你的密码 smarthive > /backup/smarthive/smarthive_$(date +\%Y\%m\%d).sql
```

### 9.4 数据恢复

```bash
mysql -u smarthive -p smarthive < /backup/smarthive/smarthive_20260401.sql
```

---

## 十、更新部署

### 10.1 标准更新流程

```bash
# 1. 备份当前版本
cp -r /www/wwwroot/smarthive /www/wwwroot/smarthive_backup_$(date +%Y%m%d)

# 2. 上传新文件（覆盖 dist/ 和 dist-server/）

# 3. 安装新依赖（如有）
cd /www/wwwroot/smarthive
npm ci --production

# 4. 重启服务
pm2 restart smarthive-backend

# 5. 验证部署
curl https://你的域名/api/health
```

### 10.2 快速回滚

```bash
# 恢复备份
rm -rf /www/wwwroot/smarthive/dist
rm -rf /www/wwwroot/smarthive/dist-server
cp -r /www/wwwroot/smarthive_backup_YYYYMMDD/dist /www/wwwroot/smarthive/
cp -r /www/wwwroot/smarthive_backup_YYYYMMDD/dist-server /www/wwwroot/smarthive/

# 重启服务
pm2 restart smarthive-backend
```

---

## 十一、常见问题

### 11.1 数据库连接失败

**症状**: `databaseConnected: false`

**排查**:
```bash
# 检查 MySQL 服务
sudo systemctl status mysqld

# 检查用户权限
mysql -u smarthive -p -h localhost smarthive

# 检查 .env 配置
cat /www/wwwroot/smarthive/.env | grep DB_
```

### 11.2 前端页面空白

**症状**: 访问域名显示空白页

**排查**:
```bash
# 检查 Nginx 配置
sudo nginx -t

# 检查静态文件
ls -la /www/wwwroot/smarthive/dist/

# 检查 Nginx 错误日志
tail -f /var/log/nginx/smarthive.error.log
```

### 11.3 API 返回 401

**症状**: API 请求返回 Unauthorized

**排查**:
```bash
# 检查 API_TOKEN 配置
cat /www/wwwroot/smarthive/.env | grep API_TOKEN

# 测试 API
curl -H "Authorization: Bearer 你的API_TOKEN" \
  https://你的域名/api/health
```

### 11.4 视频流无法播放

**症状**: 视觉页面黑屏

**排查**:
```bash
# 检查视频流端点
curl -I https://你的域名/api/vision/stream.mjpg

# 检查树莓派是否上传
# 在树莓派上执行
curl -I https://你的域名/api/health

# 检查后端日志
pm2 logs smarthive-backend | grep vision
```

### 11.5 SSE 连接断开

**症状**: 实时数据不更新

**排查**:
```bash
# 检查 SSE 端点
curl -N -H "Authorization: Bearer 你的API_TOKEN" \
  https://你的域名/api/iot/stream

# 检查 Nginx 配置是否关闭缓冲
grep -A5 "api/iot/stream" /etc/nginx/conf.d/smarthive.conf
```

---

## 十二、安全加固

### 12.1 安全检查清单

- [ ] 修改 `API_TOKEN` 为至少16位随机字符串
- [ ] 配置 HTTPS 证书
- [ ] 修改管理员默认密码
- [ ] 数据库使用强密码
- [ ] 后端端口 3001 仅内网访问
- [ ] 配置 MQTT 认证（如需公网访问）
- [ ] 定期备份数据库
- [ ] 配置日志轮转

### 12.2 生成安全 Token

```bash
# 生成16位随机字符串
openssl rand -hex 8

# 或使用 Node.js
node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
```

### 12.3 日志轮转配置

创建 `/etc/logrotate.d/smarthive`:

```
/var/log/nginx/smarthive.*.log {
    daily
    rotate 30
    compress
    delaycompress
    missingok
    notifempty
    create 0644 nginx nginx
    sharedscripts
    postrotate
        [ -f /var/run/nginx.pid ] && kill -USR1 `cat /var/run/nginx.pid`
    endscript
}
```

---

## 十三、性能优化

### 13.1 MySQL 优化

编辑 `/etc/my.cnf`:

```ini
[mysqld]
# 缓冲池大小（建议为物理内存的70%）
innodb_buffer_pool_size = 1G

# 日志文件大小
innodb_log_file_size = 256M

# 连接数
max_connections = 200

# 查询缓存
query_cache_size = 64M
query_cache_type = 1
```

### 13.2 Nginx 优化

编辑 `/etc/nginx/nginx.conf`:

```nginx
worker_processes auto;
worker_connections 1024;

gzip on;
gzip_types text/plain text/css application/json application/javascript text/xml;
gzip_min_length 1000;

keepalive_timeout 65;
```

### 13.3 PM2 集群模式（可选）

编辑 `ecosystem.config.js`:

```javascript
{
  "apps": [{
    "name": "smarthive-backend",
    "script": "server.cjs",
    "instances": "max",  // 使用所有CPU核心
    "exec_mode": "cluster",
    // ...
  }]
}
```

---

## 十四、联系与支持

如遇部署问题，请检查：

1. 后端日志: `pm2 logs smarthive-backend`
2. Nginx 日志: `/var/log/nginx/smarthive.error.log`
3. MySQL 日志: `/var/log/mysqld.log`

参考文档：
- 用户手册: `SMART_HIVE_USER_MANUAL.md`
- 项目评估: `项目落地评估报告_2026-03-20.md`
- 数据映射: `数据映射与完整性保障方案.md`
